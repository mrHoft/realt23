{
  "name": "realt23",
  "version": "0.0.3",
  "scripts": {
    "start": "ng serve realt23 --port 3000",
    "promo": "ng serve promo --port 3000",
    "build": "ng build realt23 && ng build promo",
    "deploy:nichost": "scp -o HostKeyAlgorithms=+ssh-rsa -o PubkeyAcceptedKeyTypes=+ssh-rsa -r dist/* rea7577906@ssh.rea7577906.nichost.ru:realt23.ru/docs",
    "deploy:pages": "npm run build && git checkout gh-pages && rm -f main-*.js && rm -f styles-*.css && cp -r dist/* . && git add . && git commit -m 'feat: deploy' && git push --set-upstream origin gh-pages"
  },
  "prettier": {
    "overrides": [
      {
        "files": "*.html",
        "options": {
          "parser": "angular"
        }
      }
    ]
  },
  "private": true,
  "dependencies": {
    "@angular/common": "^20.1.0",
    "@angular/compiler": "^20.1.0",
    "@angular/core": "^20.1.0",
    "@angular/forms": "^20.1.0",
    "@angular/platform-browser": "^20.1.0",
    "@angular/router": "^20.1.0",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@angular/build": "^20.1.0",
    "@angular/cli": "^20.1.0",
    "@angular/compiler-cli": "^20.1.0",
    "@tailwindcss/cli": "^4.2.1",
    "@tailwindcss/postcss": "^4.2.1",
    "autoprefixer": "^10.4.27",
    "ng-packagr": "^20.3.0",
    "tailwindcss": "^4.2.1",
    "typescript": "~5.8.2"
  }
}
